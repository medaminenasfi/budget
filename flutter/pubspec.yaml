name: smart_budget_manager
description: "A local-first personal finance manager."
publish_to: 'none'

version: 2.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  drift: ^2.20.0
  fl_chart: ^0.69.0
  flutter_riverpod: ^2.5.1
  image_picker: ^1.1.2
  path: ^1.9.0
  path_provider: ^2.1.4
  sqlite3_flutter_libs: ^0.5.24
  shared_preferences: ^2.3.2
  local_auth: ^2.3.0
  crypto: ^3.0.3
  flutter_secure_storage: ^9.2.2
  google_sign_in: ^6.2.2
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0
  build_runner: ^2.4.12
  drift_dev: ^2.20.0

flutter:
  uses-material-design: true
